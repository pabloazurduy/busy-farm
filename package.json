{
  "name": "busy-farm",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "A Firefox-first website blocker synchronized with BUSY Bar focus sessions.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "node scripts/clean.mjs",
    "test": "node --test tests/*.test.js",
    "validate": "node scripts/validate-build.mjs",
    "check": "npm run test && npm run build && npm run validate",
    "simulator": "node tests/simulator/server.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
